import React from 'react'

class Tabs extends React.Component {
    constructor(props) {
        super(props);
    }

    render = () => {
        return(
            // <div>
            //     <ul>
            //         {this.props.map((tab, idx) => {
            //             return <li key={i} tab={tab}>{tab.title}</li>
            //         })}    
            //     </ul>  
            // </div>
        )
    }
}